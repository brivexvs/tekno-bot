const chalk = require('chalk');
module.exports = async() => {
    function stack(error) {
        console.log(chalk.bgRed('ERROR - ADVANCED ERRORS'))
        console.log(chalk.white(error.stack))
        console.log(chalk.bgRed('ERROR - ADVANCED ERRORS'))
    }
}