const chalk = require('chalk');
module.exports = async() => {
    function syntaxerror(error) {
        console.log(chalk.bgRed('SYNTAX ERROR - ADVANCED ERRORS'))
        console.log(chalk.white(error))
        console.log(chalk.bgRed('SYNTAX ERROR - ADVANCED ERRORS'))
    }
}