const chalk = require('chalk');
module.exports = async() => {
    function rangeerror(error) {
        console.log(chalk.bgRed('RANGE ERROR - ADVANCED ERRORS'))
        console.log(chalk.white(error))
        console.log(chalk.bgRed('RANGE ERROR - ADVANCED ERRORS'))
    }
}