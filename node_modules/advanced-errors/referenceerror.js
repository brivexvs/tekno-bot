const chalk = require('chalk');
module.exports = async() => {
    function referenceerror(error) {
        console.log(chalk.bgRed('REFFERENCE ERROR - ADVANCED ERRORS'))
        console.log(chalk.white(error))
        console.log(chalk.bgRed('REFERENCE ERROR - ADVANCED ERRORS'))
    }
}